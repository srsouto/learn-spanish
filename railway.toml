[build]
builder = "dockerfile"

# Single service: python bot.py
# The proactive scheduler (9am + 6pm Pacific) runs as a JobQueue inside the bot process.
# No separate cron service needed — no volume sharing required.
